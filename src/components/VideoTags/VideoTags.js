import React from 'react';
import './VideoTags.css';

const VideoTags = () => (
    <div className="VideoTags" data-testid="VideoTags">
        VideoTags Component
    </div>
);

VideoTags.propTypes = {};

VideoTags.defaultProps = {};

export default VideoTags;
