import React from 'react';
import './Tabs.css';

const Tabs = () => (
    <div className="Tabs" data-testid="Tabs">
        Tabs Component
    </div>
);

Tabs.propTypes = {};

Tabs.defaultProps = {};

export default Tabs;
